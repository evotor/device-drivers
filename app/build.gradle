apply plugin: 'com.android.library'

group = 'com.github.evotor'

android {
    namespace = "ru.evotor.devices.drivers"
    compileSdk 35
    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 35
        versionCode 3_04_00_0
        versionName "3.4.0"

        testInstrumentationRunner = 'androidx.test.runner.AndroidJUnitRunner'

    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    buildFeatures {
        aidl = true
    }
}


apply plugin: 'maven-publish'
tasks.register("androidSourcesJar", Jar) {
    archiveClassifier.set("sources")
    // Указываем путь к исходникам библиотеки
    from(android.sourceSets.getByName("main").java.srcDirs)
}
afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                from components.findByName('release')
                groupId = 'com.github.evotor'
                artifactId = 'device-drivers'
                version = 'v' + android.defaultConfig.versionName
                artifact tasks.named('bundleReleaseAar')
                artifact tasks.named('androidSourcesJar')
            }
        }
    }
}

dependencies {
    implementation 'org.jetbrains:annotations:15.0'

    testImplementation 'junit:junit:4.13.2'
    //noinspection GradleDependency
    testImplementation 'androidx.test:runner:1.6.2'
    //noinspection GradleDependency
    testImplementation 'androidx.test.ext:junit:1.2.1'

    androidTestImplementation 'junit:junit:4.13.2'
    //noinspection GradleDependency
    androidTestImplementation 'androidx.test:runner:1.6.2'
    //noinspection GradleDependency
    androidTestImplementation 'androidx.test.ext:junit:1.2.1'

}
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}



